# Copy this file to deploy/worker-secrets.env.
# These secrets must never be placed in deploy/app.env or the API container.
STELLAR_TREASURY_SECRET=S_REPLACE_WITH_TESTNET_SECRET
OFFRAMP_DEPOSIT_SECRET=S_REPLACE_WITH_DEPOSIT_SECRET
